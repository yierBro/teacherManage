<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cpx.mapper.UserMapper">

    <select id="login" resultType="com.cpx.entity.User">
        select id, username, password, real_name as realName
        from user
        where username = #{username} and password = #{password}
    </select>

    <select id="findByUsername" resultType="com.cpx.entity.User">
        select id, username, password, real_name as realName
        from user
        where username = #{username}
    </select>

    <insert id="insert" parameterType="com.cpx.entity.User" useGeneratedKeys="true" keyProperty="id">
        insert into user(username, password, real_name)
        values(#{username}, #{password}, #{realName})
    </insert>

</mapper>
